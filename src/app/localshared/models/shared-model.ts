export enum DirectionEnum {
  UP = 'Up',
  DOWN = 'Down',
  LEFT = 'Left',
  RIGHT = 'Right',
}

export enum ColorEnum {
  // Select = '',
  RED = 'Red',
  YELLOW = 'Yellow',
  BLUE = 'Blue',
  GREEN = 'Green',
}

export interface TestObject {
  rank?: string;
  enName?: string;
  frName?: string;
}
